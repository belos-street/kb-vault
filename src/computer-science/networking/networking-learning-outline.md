# 计算机网络学习大纲（408 考研）

> 按照 408 计算机学科专业基础 — 计算机网络部分设计。以 TCP/IP 五层模型为主线，覆盖物理层到应用层的核心协议与原理，侧重协议机制理解和报文分析。

---

## 📌 元信息

| 项目 | 说明 |
|------|------|
| **考试大纲** | 408 计算机学科专业基础 — 计算机网络部分 |
| **分值占比** | 约 25 分 / 150 分（选择题为主，偶有综合题） |
| **目标读者** | 408 考研备考者、需要深入理解网络协议的开发者 |
| **前置知识** | 计算机组成原理（数据表示、总线）、操作系统（进程、I/O） |
| **配套教材** | 谢希仁《计算机网络》（第八版）/ 王道考研计算机网络 |

---

## 🎯 学习目标

完成本模块学习后，你应该能够：

1. 描述 TCP/IP 五层模型各层的功能与协议
2. 掌握数据链路层的帧封装、差错控制、介质访问控制（CSMA/CD）
3. 理解网络层 IP 编址、子网划分、路由算法
4. 掌握 TCP 连接管理（三次握手/四次挥手）、可靠传输、拥塞控制
5. 理解应用层协议（DNS、HTTP、FTP、SMTP）的工作机制
6. 能分析给定场景下的报文交互过程

---

## 📋 408 考纲知识点分布

| 章节 | 核心考点 | 题型倾向 | 重要度 |
|------|---------|---------|--------|
| 计算机网络体系结构 | 分层模型、协议/接口/服务 | 选择题 | ⭐⭐ |
| 物理层 | 奈奎斯特/香农定理、编码与调制 | 选择题 | ⭐⭐ |
| 数据链路层 | CSMA/CD、PPP、以太网帧格式 | 选择题 | ⭐⭐⭐ |
| 网络层 | IP 编址、子网划分、路由协议 | 选择 + 综合题 | ⭐⭐⭐⭐⭐ |
| 传输层 | TCP 三次握手/四次挥手、拥塞控制 | 选择 + 综合题 | ⭐⭐⭐⭐⭐ |
| 应用层 | DNS、HTTP、DHCP | 选择题为主 | ⭐⭐⭐ |

---

## 🗺️ 学习路径

| 阶段 | 主题 | 产出 |
|------|------|------|
| **第 1 章** | 体系结构 | 理解分层思想、五层模型各层职责 |
| **第 2 章** | 物理层 | 掌握信道容量计算、编码方式 |
| **第 3 章** | 数据链路层 | 理解帧封装、CSMA/CD 工作原理 |
| **第 4 章** | 网络层 | 能完成 IP 子网划分、理解路由算法 |
| **第 5 章** | 传输层 | 掌握 TCP 连接管理与拥塞控制机制 |
| **第 6 章** | 应用层 | 理解 DNS 解析流程、HTTP 请求/响应 |

---

## 📚 文档目录规划

```text
src/computer-science/networking/
├── networking-learning-outline.md            # 本文件（学习大纲）
├── doc/
│   ├── 01-architecture.md                    # 体系结构与分层模型
│   ├── 02-physical-layer.md                  # 物理层
│   ├── 03-data-link-layer.md                 # 数据链路层
│   ├── 04-network-layer.md                   # 网络层
│   ├── 05-transport-layer.md                 # 传输层
│   └── 06-application-layer.md               # 应用层
└── assets/                                   # 协议报文图、网络拓扑图
```

---

## 📖 各章知识点细化

### 第 1 章：计算机网络体系结构

- 计算机网络的概念、组成、功能
- 分类：LAN、MAN、WAN；按拓扑分类
- 分层模型：
  - OSI 七层 vs TCP/IP 五层（重点掌握五层）
  - 各层功能：应用层 → 传输层 → 网络层 → 数据链路层 → 物理层
- 协议三要素：语法、语义、同步
- 接口与服务：SAP、PDU（协议数据单元）
- 封装与解封装过程

### 第 2 章：物理层

- 通信基础：
  - 数据通信模型（源 → 发送器 → 信道 → 接收器 → 目的）
  - 奈奎斯特定理：极限数据传输速率 = $2W \log_2 V$
  - 香农定理：极限信息传输速率 = $W \log_2(1 + S/N)$
- 编码与调制：
  - 数字数据 → 数字信号：NRZ、曼彻斯特编码、差分曼彻斯特
  - 数字数据 → 模拟信号：ASK、FSK、PSK、QAM
- 传输介质：双绞线、同轴电缆、光纤、无线
- 物理层设备：中继器、集线器（Hub）

### 第 3 章：数据链路层

- 封装成帧与透明传输：
  - 字符计数法、字符填充、零比特填充
- 差错控制：
  - 检错编码：奇偶校验、CRC 循环冗余校验
  - 纠错编码：海明码
- 流量控制与可靠传输：
  - 停-等协议
  - 后退 N 帧（GBN）
  - 选择重传（SR）
  - 滑动窗口机制
- 介质访问控制（MAC）：
  - 静态划分：FDMA、TDMA、CDMA
  - **CSMA/CD**（以太网核心）：争用期、最小帧长、最大传输距离
  - CSMA/CA（无线局域网）
- 局域网：
  - 以太网帧格式（IEEE 802.3）
  - MAC 地址（48 位）
  - 交换机工作原理（自学习、转发、过滤）
  - VLAN（虚拟局域网）
- PPP 协议：帧格式、链路建立过程
- 数据链路层设备：网桥、交换机

### 第 4 章：网络层

- 网络层功能：路由选择、分组转发、拥塞控制
- IPv4：
  - IP 数据报格式（版本、首部长度、TTL、协议字段）
  - **IPv4 分片与重组**：MTU、标识字段、片偏移、MF/DF 标志
  - **IP 地址分类**：A/B/C/D/E 类
  - **子网划分与 CIDR**：子网掩码、前缀长度、超网
  - 特殊地址：网络地址、广播地址、环回地址、私有地址
  - NAT（网络地址转换）
- IPv6：
  - 128 位地址、表示法
  - IPv4 → IPv6 过渡：双栈、隧道
- 路由算法与协议：
  - 静态路由 vs 动态路由
  - RIP（距离向量、跳数、最大 15 跳）
  - OSPF（链路状态、Dijkstra、区域划分）
  - BGP（路径向量、AS 间路由）
- 路由器：
  - 路由表与转发过程
  - 最长前缀匹配
- ICMP：
  - 差错报告（目的不可达、超时、重定向）
  - 询问报文（ping、traceroute）
- ARP：IP → MAC 地址解析
- DHCP：动态主机配置（DORA 四步）
- IGMP：组播管理

### 第 5 章：传输层

- 传输层功能：进程间通信、复用与分用
- UDP：
  - 无连接、不可靠、面向报文
  - UDP 首部格式（源端口、目的端口、长度、校验和）
  - 适用场景：DNS、视频流、实时通信
- TCP：
  - 面向连接、可靠传输、面向字节流
  - TCP 报文段格式（序号、确认号、窗口、标志位）
  - **连接管理**：
    - 三次握手（SYN → SYN+ACK → ACK）
    - 四次挥手（FIN → ACK → FIN → ACK）
    - TIME_WAIT（2MSL）的作用
  - **可靠传输机制**：
    - 序号与确认
    - 超时重传
    - 快速重传（3 个重复 ACK）
    - 流量控制（滑动窗口）
  - **拥塞控制**：
    - 慢开始（cwnd 指数增长）
    - 拥塞避免（cwnd 线性增长）
    - 快恢复（ssthresh 减半，cwnd = ssthresh）
    - 拥塞窗口 vs 发送窗口（发送窗口 = min(rwnd, cwnd)）
- 端口号：知名端口（0-1023）、注册端口、动态端口

### 第 6 章：应用层

- 网络应用模型：C/S、P2P、混合
- DNS（域名系统）：
  - 域名层次结构（根 → 顶级域 → 二级域）
  - 解析流程：递归查询 + 迭代查询
  - DNS 报文格式
  - DNS 使用 UDP（查询）和 TCP（区域传送）
- HTTP（超文本传输协议）：
  - HTTP/1.0 vs HTTP/1.1（持久连接、管道化）
  - 请求方法：GET、POST、PUT、DELETE、HEAD
  - 状态码：1xx/2xx/3xx/4xx/5xx
  - Cookie 与 Session
  - HTTPS = HTTP + TLS/SSL
- FTP（文件传输协议）：
  - 控制连接（21 端口）+ 数据连接（20 端口）
  - 主动模式 vs 被动模式
- 电子邮件：
  - SMTP（发送，25 端口）
  - POP3（接收，110 端口）/ IMAP（143 端口）
- DHCP 应用层视角（与第 4 章呼应）

---

## 📐 高频计算题型

| 题型 | 涉及章节 | 关键公式/方法 |
|------|---------|--------------|
| 信道容量计算 | 第 2 章 | 奈奎斯特： $C = 2W \log_2 V$；香农： $C = W \log_2(1+S/N)$ |
| CRC 校验计算 | 第 3 章 | 模 2 除法，生成多项式 |
| 最小帧长计算 | 第 3 章 | 最小帧长 = 2 × 传播时延 × 数据传输速率 |
| 子网划分 | 第 4 章 | 借主机位做子网位、计算可用主机数 |
| 路由表匹配 | 第 4 章 | 最长前缀匹配 |
| TCP 拥塞窗口 | 第 5 章 | 慢开始/拥塞避免/快恢复的 cwnd 变化 |

---

## ❓ 408 高频真题考点

1. **子网划分与 CIDR**：给定 IP 和掩码，判断网络地址、广播地址、可用主机数
2. **路由聚合**：多个子网聚合为超网
3. **TCP 三次握手/四次挥手**：各标志位状态、为什么不能两次握手
4. **TCP 拥塞控制**：给定事件序列，画出 cwnd 变化图
5. **CSMA/CD**：最小帧长、争用期计算
6. **HTTP 与 DNS**：URL 解析全过程（DNS → TCP → HTTP）
7. **ARP 工作过程**：ARP 请求/应答的广播与单播
8. **滑动窗口**：GBN 与 SR 的窗口大小限制

---

## ✅ 完成标准

- [ ] 能画出 TCP/IP 五层模型并说明各层 PDU 名称
- [ ] 能用奈奎斯特/香农公式计算信道容量
- [ ] 能解释 CSMA/CD 的工作流程并计算最小帧长
- [ ] 能完成 IP 子网划分（含 CIDR 聚合）
- [ ] 能描述 RIP/OSPF/BGP 的区别与适用场景
- [ ] 能画出 TCP 三次握手和四次挥手的报文交互
- [ ] 能画出 TCP 拥塞控制的 cwnd 变化曲线
- [ ] 能描述 DNS 递归 + 迭代解析的完整流程
- [ ] 能区分 HTTP/1.0 与 HTTP/1.1 的关键差异

---

## 🔗 关联模块

- 操作系统：[operating-system/](../operating-system/) 第 9 章 I/O 管理（Socket、I/O 多路复用）
- 计算机组成原理：[computer-organization/](../computer-organization/) 数据表示、总线传输
- 实操互补：[deploy/network/](../../deploy/network/) DNS/HTTP/HTTPS 排障实操
- 实操互补：[deploy/nginx/](../../deploy/nginx/) 反向代理与负载均衡

---

## 📝 学习建议

1. **网络层和传输层是核心**：占 408 网络分值约 60%，子网划分和 TCP 机制几乎每年必考
2. **协议要画图**：三次握手、四次挥手、DNS 解析流程，画出来比背文字有效 10 倍
3. **子网划分靠练**：这是纯计算题，做 10 道真题就能熟练
4. **拥塞控制要理解状态机**：慢开始 → 拥塞避免 → 快恢复，记住触发条件和 cwnd 变化规则
5. **应用层偏记忆**：端口号、协议特点记住即可，不需要深入实现细节
6. **与实操结合**：学完理论后用 `wireshark` 抓包看真实报文，理解会更深
