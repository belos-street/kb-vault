# LLM API 密钥（必填）
OPENAI_API_KEY=sk-your-api-key-here

# API 端点（可选，默认 DeepSeek）
OPENAI_BASE_URL=https://api.deepseek.com

# 模型名（可选；createAgent 字符串简写必须带 provider 前缀，如 openai:gpt-5.4）
DEFAULT_MODEL=openai:deepseek-v4-flash

# 意图分类器模型（可选；缺省复用 DEFAULT_MODEL）
# CLASSIFIER_MODEL=openai:deepseek-v4-flash
